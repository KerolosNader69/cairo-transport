{
  "name": "cairo-transport",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Cairo-Transport-main/frontend && npm install && npm run build",
    "vercel-build": "cd Cairo-Transport-main/frontend && npm install && npm run build"
  }
}
